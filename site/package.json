{
  "name": "mutelet-site",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "capture-assets": "./scripts/capture-assets.sh",
    "check": "node scripts/check-site.mjs",
    "test": "pnpm run build && pnpm run check"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6"
  }
}
