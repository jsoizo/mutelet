{
  "name": "mutelet-site",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "test": "pnpm run build && node scripts/check-site.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6"
  }
}
